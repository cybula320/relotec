@tailwind base;
@tailwind components;
@tailwind utilities;

/* (opcjonalnie) Twoje niestandardowe style */
@layer components {
  .widget-card {
    @apply rounded-xl shadow-sm bg-white dark:bg-gray-900 p-6;
  }
}